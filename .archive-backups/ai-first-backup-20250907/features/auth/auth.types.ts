/**
 * Auth Types
 */

export const AI_INTERFACE = {
  purpose: "Type definitions for auth feature",
  exports: ["AuthProps", "AuthState"],
  patterns: ["types-only"]
};

export interface AuthProps {
  // TODO: Define auth props
}

export interface AuthState {
  // TODO: Define auth state
}
