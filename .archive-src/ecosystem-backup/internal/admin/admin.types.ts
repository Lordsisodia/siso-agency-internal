/**
 * Admin Types
 */

export const AI_INTERFACE = {
  purpose: "Type definitions for admin feature",
  exports: ["AdminProps", "AdminState"],
  patterns: ["types-only"]
};

export interface AdminProps {
  // TODO: Define admin props
}

export interface AdminState {
  // TODO: Define admin state
}
