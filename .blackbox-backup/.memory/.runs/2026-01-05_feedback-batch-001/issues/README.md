# Issues

Each issue is a folder containing:

- `plan.md` — the primary artifact (problem → options → chosen approach → steps → verification)
- `context.md` — optional scratchpad for logs, links, screenshots references

Create new issues with:

`node .blackbox/bin/blackbox.mjs new "Title" --domain ui --priority p1`

