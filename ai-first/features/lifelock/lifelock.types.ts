/**
 * Lifelock Types
 */

export const AI_INTERFACE = {
  purpose: "Type definitions for lifelock feature",
  exports: ["LifelockProps", "LifelockState"],
  patterns: ["types-only"]
};

export interface LifelockProps {
  // TODO: Define lifelock props
}

export interface LifelockState {
  // TODO: Define lifelock state
}
