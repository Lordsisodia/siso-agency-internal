# Execution Pipeline

This directory will contain the execution pipeline for SISO Internal.

## Status
Empty - pending import from Agent_OS.

## To Import
When ready, copy the execution pipeline from:
```
~/SISO_Workspace/Agent_OS/agent_ecosystem/standard_modules/execution_pipeline/
```

This includes:
- `agents/` - All pipeline agents
- `workflow.yml` - Pipeline definition
- `HANDOFF.md` - Agent handoff mechanism
- `docs/`, `schemas/`, `scripts/`

## Why
Keep SISO Internal separate from the core Agent_OS execution pipeline until it's tested and ready.
