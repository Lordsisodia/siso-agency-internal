{
  "name": "claudia-lambda-demo",
  "version": "1.0.0",
  "description": "Demo Lambda function with local testing",
  "main": "lambda.js",
  "scripts": {
    "start": "node local-server.js",
    "dev": "nodemon local-server.js",
    "deploy": "claudia create --region us-east-1 --api-module app",
    "update": "claudia update"
  },
  "dependencies": {
    "claudia-api-builder": "^4.1.2",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}