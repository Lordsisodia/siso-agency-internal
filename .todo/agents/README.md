# Agents

Each agent has:

- a sprint board (`*.sprints.md`)
- a skills file (`*.skills.md`)

These files should link back to:
- the blackbox role runbook in `.blackbox/agents/`
- relevant reusable skills in `.blackbox/.skills/`

