# Platform
System-level docs: architecture, migrations, offline strategy, BMAD.

- `architecture/`
  - `decisions/`, `patterns/`, `system-maps/`, `risk-reviews/`
  - Domains: `lifelock/`, `database/`, `ecosystem/`, `partner/`, `duplication/`, `tasks/`, `offline-sync/`, `misc/`
- `migration-reports/`
  - `completion/` — completion/health/final reports
  - `lifelock/` — LifeLock-specific migration summaries
- `offline/` — offline/Supabase reliability (`strategy/`, `implementation/`, `testing/`)
- `bmad/` — BMAD framework and outputs
