# Steps

Each file in this folder is **one step**.

Naming convention:

- `0001_<slug>.md`
- `0002_<slug>.md`
- ...

Create steps using:

```bash
./.blackbox/scripts/new-step.sh --plan .blackbox/agents/.plans/<plan> "short step title"
```

