/**
 * Dashboard Types
 */

export const AI_INTERFACE = {
  purpose: "Type definitions for dashboard feature",
  exports: ["DashboardProps", "DashboardState"],
  patterns: ["types-only"]
};

export type DashboardProps = object;

export type DashboardState = object;
