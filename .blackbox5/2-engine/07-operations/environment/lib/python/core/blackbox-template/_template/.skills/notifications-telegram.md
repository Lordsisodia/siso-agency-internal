# Skill: Telegram Notifications

Use for long-running work where a human wants updates.

Security:
- Never commit tokens
- Use env vars (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`) or a local gitignored markdown config at `.blackbox/.local/telegram.md`
